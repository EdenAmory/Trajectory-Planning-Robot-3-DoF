<Window x:Class="Trajectory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KELOMPOK 5 RNO" Width="1000" Height="700" ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen" Background="#FFEFEFEF">
    <Window.Resources>
        <!-- tiled grid brush: adjust TileSize to control spacing -->
        <DrawingBrush x:Key="GridBrush" TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute"
                      Viewbox="0,0,20,20" ViewboxUnits="Absolute" Stretch="None">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <!-- light square background -->
                    <GeometryDrawing Brush="White">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="0,0,20,20" />
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>

                    <!-- thin grid lines -->
                    <GeometryDrawing>
                        <GeometryDrawing.Pen>
                            <Pen Brush="Gray" Thickness="0.5" />
                        </GeometryDrawing.Pen>
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <LineGeometry StartPoint="0,0" EndPoint="20,0"/>
                                <LineGeometry StartPoint="0,0" EndPoint="0,20"/>
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>
    </Window.Resources>

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <!-- reduced center and right columns so right pane moves inward -->
            <ColumnDefinition Width="73*" />
            <ColumnDefinition Width="469*"/>
            <ColumnDefinition Width="442*" />
        </Grid.ColumnDefinitions>

        <!-- Left: Chart + Initialization -->
        <Grid Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <!-- Chart -->
                <RowDefinition Height="140" />
                <!-- Initialization panel -->
            </Grid.RowDefinitions>

            <!-- Chart area: grid + centered axes + overlay canvases for path and arm -->
            <Border Grid.Row="0" BorderBrush="#FF9E9E9E" BorderThickness="1" Background="White" CornerRadius="2" Padding="6" Margin="2,2,2,2">
                <Grid Name="m_chart" Background="Transparent" ClipToBounds="True">

                    <Line Stroke="#AA777777" StrokeThickness="1" X1="0" X2="1"
                          Stretch="Fill" StrokeDashArray="2,2"
                          VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                    <Line Stroke="#AA777777" StrokeThickness="1" Y1="0" Y2="1"
                          Stretch="Fill" StrokeDashArray="2,2"
                          HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

                    <Canvas Name="m_pathCanvas" IsHitTestVisible="False"/>
                    <Canvas Name="m_armCanvas" IsHitTestVisible="True"/>
                </Grid>
            </Border>

            <!-- Initialization panel -->
            <Border Grid.Row="1" BorderBrush="#FFBDBDBD" BorderThickness="1" Background="#FFF7F7F7" CornerRadius="2" Padding="8" Margin="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Left: angle inputs -->
                    <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Top" Margin="0,2,12,0">
                        <TextBlock Text="Initialization" FontWeight="Bold" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="a1" Width="28" VerticalAlignment="Center"/>
                            <TextBox Name="m_a1" Width="70" Text="120" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="a2" Width="28" VerticalAlignment="Center"/>
                            <TextBox Name="m_a2" Width="70" Text="128" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="a3" Width="28" VerticalAlignment="Center"/>
                            <TextBox Name="m_a3" Width="70" Text="85" />
                        </StackPanel>
                    </StackPanel>

                    <!-- Middle: parameters -->
                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Top">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,6">
                            <TextBlock Text="Titik (n)" Width="70" VerticalAlignment="Center"/>
                            <TextBox Name="m_n" Width="80" Text="25"/>
                            <TextBlock Text="Waktu (ms)" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            <TextBox Name="m_t" Width="80" Text="1000"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <RadioButton Name="m_rbTimePath" Content="Time/Path" IsChecked="True" Margin="0,0,12,0"/>
                                <RadioButton Name="m_rbTimeTrack" Content="Time/Track" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <Button Name="m_clearPath" Content="Clear Path" Width="90" Height="26" Margin="0,0,8,0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Right: reserved -->
                    <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Right">
                        <!-- reserved -->
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Background image (visible behind right-side controls) -->
        <Image Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center"
               Margin="10,0,0,0" Source="/E_9Jb7_VUAgNF7Y.jpg"
               Stretch="Fill" Opacity="1.0" Width="430" />

        <!-- Right: COM controls + Joint Space and Work Space -->
        <!-- Make panel transparent so image shows through; controls that need contrast use semi-opaque backgrounds -->
        <Border Grid.Column="2" Margin="8,2,2,2" Background="Transparent" CornerRadius="6" Padding="8">
            <Border.Resources>
                <!-- Labels on top of the image: use White for better contrast against the colorful image -->
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="White"/>
                </Style>

                <!-- Inputs: semi-opaque white so text stays readable while image remains visible behind controls -->
                <Style TargetType="TextBox">
                    <Setter Property="Background" Value="#CCFFFFFF"/>
                    <!-- 80% white -->
                    <Setter Property="Foreground" Value="Black"/>
                </Style>

                <Style TargetType="ListBox">
                    <Setter Property="Background" Value="#CCFFFFFF"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Style>

                <Style TargetType="GroupBox">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Style>

                <Style TargetType="Button">
                    <Setter Property="Background" Value="#E6FFFFFF"/>
                    <!-- subtle semi-opaque -->
                    <Setter Property="Foreground" Value="Black"/>
                </Style>
            </Border.Resources>

            <StackPanel Orientation="Vertical">
                <!-- COM port + Connect controls -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8" VerticalAlignment="Top" >
                    <TextBlock Text="COM:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox Name="cmbPorts" Width="120" VerticalAlignment="Center" Margin="0,0,8,0"
                              ToolTip="Select COM port"/>
                    <Button Name="btnConnect" Content="Open Port" Width="90" Height="26" Margin="0,0,6,0"/>
                    <Button Name="btnDisconnect" Content="Close Port" Width="90" Height="26" Margin="0,0,6,0"/>
                    <Button Name="btn_originPose" Content="Origin Pose" Width="100" Height="26"/>
                </StackPanel>

                <!-- Joint Space Representation -->
                <GroupBox Header="Joint Space Representation" Margin="0,0,0,8" Padding="8" FontWeight="SemiBold" Background="Transparent">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Left column: initial + final posture and buttons -->
                        <StackPanel Grid.Column="0" Orientation="Vertical">
                            <TextBlock Text="Initial Posture" FontWeight="Bold" Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBox Name="m_teta1_0" Width="50" Margin="0,0,6,0" />
                                <TextBox Name="m_teta2_0" Width="50" Margin="0,0,6,0" />
                                <TextBox Name="m_teta3_0" Width="50" />
                            </StackPanel>

                            <TextBlock Text="Final Posture" FontWeight="Bold" Margin="6,10,0,6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBox Name="m_teta1_1" Width="50" Margin="0,0,6,0" Text="45"/>
                                <TextBox Name="m_teta2_1" Width="50" Margin="0,0,6,0" Text="45"/>
                                <TextBox Name="m_teta3_1" Width="50" Text="-30"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <Button Name="m_calcJoint" Content="Calculate" Width="80" Height="26" Margin="0,0,8,0"/>
                                <Button Name="m_runJoint" Content="Run" Width="60" Height="26"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Right column: trajectory generated list + indicator panel -->
                        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="12,0,0,0">
                            <TextBlock Text="Trajectory Generated" FontWeight="Bold" Margin="0,0,0,6"/>
                            <ListBox Name="m_joint_list" MinHeight="120" Height="140"/>

                            <!-- Indicator panel: shows Qx, Qy and joint angles for selected item -->
                            <Border BorderBrush="#FFBDBDBD" BorderThickness="1" Padding="6" Margin="0,8,0,0" Background="#CCFFFFFF">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="Selected Pose" FontWeight="Bold" Margin="0,0,0,6" Foreground="Black"/>
                                    <UniformGrid Columns="2" Rows="6" Margin="0,0,0,0">
                                        <TextBlock Text="Qx:" VerticalAlignment="Center" Foreground="Black"/>
                                        <TextBox Name="txtQx" IsReadOnly="True" />
                                        <TextBlock Text="Qy:" VerticalAlignment="Center" Foreground="Black"/>
                                        <TextBox Name="txtQy" IsReadOnly="True" />
                                        <TextBlock Text="teta1 (°):" VerticalAlignment="Center" Foreground="Black"/>
                                        <TextBox Name="txtT1" IsReadOnly="True" />
                                        <TextBlock Text="teta2 (°):" VerticalAlignment="Center" Foreground="Black"/>
                                        <TextBox Name="txtT2" IsReadOnly="True" />
                                        <TextBlock Text="teta3 (°):" VerticalAlignment="Center" Foreground="Black"/>
                                        <TextBox Name="txtT3" IsReadOnly="True" />
                                        <TextBlock Text="Orientasi (°):" VerticalAlignment="Center" Foreground="Black"/>
                                        <TextBox Name="txtOrientasi" IsReadOnly="True" />
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <!-- Work Space Representation -->
                <GroupBox Header="Work Space Representation" Padding="8" FontWeight="SemiBold" Background="Transparent">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Left: initial + final workspace posture -->
                        <StackPanel Grid.Column="0" Orientation="Vertical">
                            <TextBlock Text="Initial Posture" FontWeight="Bold" Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBox Name="m_qx_0" Width="60" Margin="0,0,6,0" />
                                <TextBox Name="m_qy_0" Width="60" Margin="0,0,6,0" />
                                <TextBox Name="m_orientasi_0" Width="60" />
                            </StackPanel>

                            <TextBlock Text="Final Posture" FontWeight="Bold" Margin="6,10,0,6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBox Name="m_qx_1" Width="60" Margin="0,0,6,0" />
                                <TextBox Name="m_qy_1" Width="60" Margin="0,0,6,0" />
                                <TextBox Name="m_orientasi_1" Width="60" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <Button Name="m_calcWork" Content="Calculate" Width="80" Height="26" Margin="0,0,8,0"/>
                                <Button Name="m_runWork" Content="Run" Width="60" Height="26"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Right: work trajectory list -->
                        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="12,0,0,0">
                            <TextBlock Text="Trajectory Generated" FontWeight="Bold" Margin="0,0,0,6"/>
                            <!-- Prevent this list from expanding the layout: limit height and enable internal scrolling -->
                            <ListBox Name="m_work_list" MinHeight="120" MaxHeight="140"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                                     ScrollViewer.CanContentScroll="True"/>

                        </StackPanel>
                    </Grid>
                </GroupBox>

                <!-- Bottom-right Exit button -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Name="m_exit" Content="Exit" Width="80" Height="28"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>